#include <stdlib.h>

#include "../functions.h"
#include "../lang/constants.h"
#include "../lang/names.h"

char *ltoc(char *lang_str)
{
    char *output = malloc(MAX_FILE_LENGTH);
    return output;
    free(output);
}